<mvc:View
    controllerName="logaligroup.invoices.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Page
        id="page"
        title="{i18n>title}"
    >
        <content>
            <Text
                id="_IDGenText"
                text="Aplicacion Ui5 Reto Logali Group"
            />
            <Panel
                id="_IDpanel01"
                headerText="{i18n>selectionScreen}"
            >
                <Label
                    id="labelShipName"
                    text="{i18n>shipName}"
                    labelFor="shipName"
                    class="sapUiSmallMargin"
                />
                <!-- con la propiedad value , el valor que llegue a este control lo pasamos a una variable
                en este caso lo colocamos en la propiedad  ShipName del modelo selectionScreen--> 
                <Input
                    id="shipName"                    
                    value="{selectionScreen>/ShipName}"
                    width="200px"
                    class="sapUiSmallMarginEnd"
                />
                <Label
                    id="labelCountry"
                    text="{i18n>country}"
                    class="sapUiSmallMargin"
                />
                 <!-- con la propiedad selectedKey , el valor del key seleccionado lo enviamos a una variable
                en este caso lo colocamos en la propiedad  CountryKey del modelo selectionScreen--> 
                <Select 
                    id="slCountry" 
                    selectedKey="{selectionScreen>/CountryKey}" 
                    forceSelection="false"
                    items="{selectionScreen>/Countries}"
                    visible="true">
                    <items>
                    <core:Item id="_IDGenItem" key="{selectionScreen>key}" text="{selectionScreen>text}"/>
                    </items>
                </Select>
                <Button 
                    id="btnFilter" 
                    text="{i18n>btnFilter}" 
                    icon="sap-icon://filter" 
                    press="onFilter" 
                    class="sapUiSmallMarginEnd"/>

                <Button 
                    id="btnClearFilter" 
                    text="{i18n>btnClearFilter}" 
                    icon="sap-icon://clear-filter" 
                    press="onFilterClear" 
                    class="sapUiSmallMarginEnd"/>    
                
            </Panel>
        </content>
    </Page>
</mvc:View>
